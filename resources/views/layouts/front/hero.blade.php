<section class="mb-30px">
    <div class="container">
        <div class="hero-banner">
            <div class="hero-banner__content">
                <h3>@yield('hero-title')</h3>
                <h1>@yield('hero-description')</h1>
                <h4>@yield('hero-date')</h4>
                @yield('hero-link')
            </div>
        </div>
    </div>
</section>
